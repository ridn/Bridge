package Bridge;

//import java.util.Arrays;

public class DeckReader {
	public static void main(String args[]) {
		/*
		Game game = new Game();
		Deck deck = new Deck();
		System.out.println("New Deck:\n" + Arrays.toString(deck.deckArray));
		deck.shuffleDeck();
		System.out.println("Shuffled Deck:\n" + Arrays.toString(deck.deckArray));
		new Player("Dan",game,false);
		new Player("Tyler",game,true);
		new Player("Not Neil",game,true);
		new Player("OG Bobby Johnson",game,true);
		deck.dealToGame(game);
		for(int i = 0; i < game.getNumberOfPlayers(); i++)
			System.out.println(game.playerWithNumber(i+1).toString());
		while(game.playerWithNumber(1).hand.size() > 0)
			game.playTurn();
		for(int i = 0; i < game.getNumberOfPlayers(); i++)
			System.out.println(game.playerWithNumber(i+1).toString());
			*/
	}
}
